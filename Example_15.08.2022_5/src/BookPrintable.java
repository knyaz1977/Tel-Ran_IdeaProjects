
// Наследование от другого интерфейса extends
public interface BookPrintable extends Printable, A{ // в отличии от классов интерфейсы имеют множественное наследование,
    // записываются через запятую!

    void  paint();


}
